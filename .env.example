# Anthropic API key for Claude vision label analysis.
# WARNING: EXPO_PUBLIC_ vars are bundled into the app and visible to anyone
# who unpacks the binary. This setup is for local development only.
# For production, route requests through your own backend that holds the key.
EXPO_PUBLIC_ANTHROPIC_API_KEY=sk-ant-...
